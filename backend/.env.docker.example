# Backend Environment Variables for DOCKER
# Copy this file to .env when running with Docker Compose.

# Node environment
NODE_ENV=development

# Database connection details for the Docker container
# The DB_HOST must match the service name in your docker-compose.yml file (e.g., 'db' or 'mysql').
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=ats_db

# JWT
JWT_SECRET=a-very-long-and-super-secret-key-for-jwt

# Redis connection for caching
REDIS_HOST=redis
REDIS_PORT=6379